apiVersion: apps/v1
kind: Deployment
metadata:
  name: node
spec:
  selector:
    matchLabels:
      app: node
  replicas: 1
  template:
    metadata:
      labels:
        app: node
    spec:
      containers:
      - name: node
        image: node:lts-alpine
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
        command:
          - /bin/sh
          - -c
          - cp -r /tmp/app /; cd app; npm install; npm start
        volumeMounts:
          - mountPath: /tmp/app/
            name: node
      volumes:
      - name: node
        configMap:
          name: node
